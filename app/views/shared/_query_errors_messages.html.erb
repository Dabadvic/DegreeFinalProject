<% if @query.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-danger">
			Se ha encontrado <%= pluralize(@query.errors.count, "error") %>
		</div>
		<ul>
		<% @query.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
<% end %>